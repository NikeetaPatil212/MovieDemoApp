package com.example.plexigoappdemo.factory

/*
import androidx.lifecycle.ViewModel
import androidx.lifecycle.ViewModelProvider
import com.example.plexigoappdemo.repository.MoviesRepository
import com.example.plexigoappdemo.viewmodel.MoviesViewModel

class MoviesFactory(private val repo: MoviesRepository): ViewModelProvider.NewInstanceFactory() {
    override fun <T : ViewModel> create(modelClass: Class<T>): T {
        return if (modelClass.isAssignableFrom(MoviesViewModel::class.java)) {
            MoviesViewModel(repo) as T
        } else {
            throw IllegalArgumentException("ViewModel Not Found")
        }
    }
}*/
